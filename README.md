Sources for the plugin `plugin.appodeal.FacebookAudience`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.FacebookAudience" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
